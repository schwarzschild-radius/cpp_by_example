#include <iostream>

int main() {
    // Task 1: Use chained assignment to assign the same value to three variables a, b, and c.
    // TODO: Replace with correct assignment logic.
    int a, b, c;
    // a = b = c = ??;

    // Task 2: Use compound assignment operators to reduce redundancy in updating a variable.
    int score = 20;
    int bonus = 5;
    // TODO: Update score to add bonus twice using +=

    // Task 3: Use *= to compute power (e.g., square a number) with compact syntax.
    int num = 4;
    // TODO: Multiply num by itself using *= and print

    // Task 4: Swap values of x and y without using a temp variable and using only assignment operators.
    int x = 3, y = 7;
    // TODO: Perform swap using += and -= (or ^= and ^=)

    // Task 5: Given an equation result = (val + 10) * 2, convert to use only compound assignments
    int val = 5;
    int result = 0;
    // TODO: Use += and *= to match the equation

    return 0;
}

/*
Expected Output:
Task 1 -> a = b = c = 10; All should print 10
Task 2 -> score = 30
Task 3 -> num = 16
Task 4 -> x = 7, y = 3
Task 5 -> result = 30
*/
