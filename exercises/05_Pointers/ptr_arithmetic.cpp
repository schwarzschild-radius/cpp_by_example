#include <iostream>

int main() {
  int arr[] = {1, 2, 3, 4, 5};
  int *ptr = arr;

  // TODO 1: Print the first element and move the pointer forward (using
  // post-increment) Expected output: 1

  // TODO 2: Print the element two ahead from current position  (using pointer +
  // offset (i.e., arr[3])) Expected output: 4

  // TODO 3: Pre-increment the pointer and print what it points to (i.e.,
  // arr[2]) Expected output: 3

  // TODO 4: Print the current value, then move the pointer
  // backward(post-decrement) Expected output: 3

  return 0;
}
