#include <iostream>

int main() {
  int a[2][4] = {{3, 6, 9, 12}, {15, 18, 21, 24}};

  // TODO 1: Access the 3rd element in the second row (value 21) using
  // array-style indexing. Write the expression below and print it.

  // TODO 2: Now use pointer-style syntax to access the same value (21) from the
  // 2nd row. Hint: Think of *(a + row) + col

  // TODO 3: Access 3rd element of the first row using  notation: [1[a]]

  return 0;
}
